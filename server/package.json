{
  "name": "my-own-secret-space-server",
  "version": "1.0.0",
  "description": "A web app that records and remembers my day",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"tsc -w\" \"nodemon dist/main\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yong",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.15"
  }
}
